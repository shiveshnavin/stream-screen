# stream-screen
A simple http server to stream screen


### Usage
```
npx stream-screen
```

### Options
If you want to use ffmpeg available in system path.
```
npx stream-screen --use-local-ffmpeg
```
Or provide the full path to ffmpeg manually.
```
npx stream-screen --ffmpeg-path /path/to/ffmpeg
```